# 接口文档

## 一、基础信息

- 基础路径：`/api/v1`
- 响应结构：统一使用 `code/message/data`

## 二、健康检查

### GET /api/v1/health

该接口用于检查 HTTP 服务是否存活，不依赖数据库初始化，使用统一响应结构。

响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": null
}
```

## 三、业务接口

待业务模块确定后补充。
